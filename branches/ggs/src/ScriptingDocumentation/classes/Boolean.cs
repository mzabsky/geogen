/// Either true or false boolean value.
/// 
/// Boolean supports @link operators all operators@endlink expected from a Boolean type in a programming language.
/// 
/// Boolean object can be created using `true` and `false` literals:
/// 
/// @code{.cs}
/// var a = true;
/// var b = false | a;
/// @endcode
class Boolean
{
private:

    /// This fake method is here, because doxygen doesn't like classes without members.
    void FakeMethod();
public:
};
